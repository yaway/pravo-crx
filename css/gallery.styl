.layer-artworks
  layer(1)

.layer-fav
  layer(2)
  flex-direction(column)
  align-items(center)
  justify-content(flex-end)
  margin-bottom 10px
  
.btn-fav
  @extends .btn

.gallery
  color #666
  .btn-fav
    display none
  &.with-artworks
    .btn-fav
      display inline-block