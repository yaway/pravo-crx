.layer-clock
  align-items(center)
  justify-content(center)
  .time
    margin-bottom 20px
    font-size 144px
    color #fff
    &.is-dark
      palette(#fff)
    &.is-light
      palette(#000)

      
